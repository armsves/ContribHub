/** CDN dataset creation fee: 1 USDFC */
export const CDN_DATA_SET_CREATION_FEE = BigInt(1 * 10 ** 18);

/** Merkle tree leaf size (32 bytes) for storage calculations */
export const LEAF_SIZE = 32n;
